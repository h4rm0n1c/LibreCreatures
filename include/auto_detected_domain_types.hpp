#pragma once
// Deliberately empty: a stable include point for forward declarations of
// project domain types that would otherwise need adding to every header
// that could reference them. Nothing currently needs one.
